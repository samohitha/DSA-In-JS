// Arrays

// Wt are arrays

// let's say we have to show 100 numbers

// Arrays can be described as a data structures that store collection of data in a  linear orientation in the memory

// Data structure => These are different structures that we can prepare to store data in different ways  depending on our use case

// Ex : Hierarchy => c : drive => Movies => docs => downloads

// Arrays actually store store data in a linear contiguous orientation in the memory

// Examples : Images

// Image is actually combination of arrays => rgb channels

// Small small pixel values

// -----------------------How to create arrays --------------------------------//

let x = [];

// Initialization of empty array

let y = [1, 2, 45, 6, false, "bharath", NaN, null, undefined];

let z = ["ab", "cd", "ef"];

// Js supports heterogenous values

console.log(x, y, z);

// ---------------------------Array Indexing ----------------------------------//

// Individual elements of the array also got unique indexes

// Starting from zero

// arr = [1, 2, 3, 4, 5, 6]
// idx =  0  1  2  3  4  5

// U can't update the strings based on index

// strings in js are immutable

// Arrays are mutable

let str = "bharath";

str[0] = b;

console.log(str);

let a1 = [1, 3, 56, 89, 0];
a1[0] = 987;
console.log(a1);

